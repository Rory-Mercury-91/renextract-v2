# Dépendances principales
pywebview==4.4.1
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
requests==2.31.0

# Dépendances pour le service de traduction intégré
# torch==2.7.1
# transformers
# sentencepiece>=0.1.99
# protobuf>=3.20.0

# Dépendances de build (optionnelles)
pyinstaller==6.16.0  # Uncomment pour le build
